/**
 * Image processing utilities for OMR (Optical Mark Recognition)
 * This is a wrapper around the new modular OMR system for backward compatibility
 */

export { OMRProcessor } from './omr/processor';
export type { StudentInfo, ProcessingResult } from './omr/types';
